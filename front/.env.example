# Copy to .env.local and adjust. Never committed — this repo is public (TRE-5).

# Origin of the Trekker API. Must match the API's FRONTEND_URL CORS setting
# from the other side, and is allowed in the page's connect-src.
NEXT_PUBLIC_API_ORIGIN="http://localhost:6800"
