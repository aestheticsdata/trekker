export default function LoginPage() {
  return (
    <main className="flex h-screen flex-col items-center justify-center gap-3 font-mono">
      <p className="text-ink text-sm tracking-[0.2em]">TREKKER</p>
      <p className="text-ink-dim text-xs">Sign-in arrives with TRE-15.</p>
    </main>
  );
}
